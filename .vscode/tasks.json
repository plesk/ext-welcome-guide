{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "yarn build && cp dist/htdocs/bundle.js src/htdocs/bundle.js && cd src && zip -9r test.zip *",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}